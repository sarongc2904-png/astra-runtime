# Execution policy

The POC is analysis/generation only.

External side effects are disabled by default. Connectors for n8n, Meta, WhatsApp, email, CRM, calendar, or any publishing system must be read-only or stubbed until a separate execution gate is approved.

The POC may generate proposed actions, but it must not perform irreversible actions automatically.
