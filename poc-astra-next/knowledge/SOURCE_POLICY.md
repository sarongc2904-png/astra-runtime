# Source policy

Only material with a clear source and compatible usage terms is included in the portable knowledge pack. External repositories are referenced or selectively adapted; they are not silently copied wholesale.

ASTRA-specific knowledge must be separable from runtime implementation. Each migrated unit should record source path, purpose, and whether it is a user fact, methodology, heuristic, benchmark rule, or proposed default.
